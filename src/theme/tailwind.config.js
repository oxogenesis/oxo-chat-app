/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
    },
  }
}

// 默认容器：h-full bg-neutral-200 dark:bg-neutral-800 p-5px
// 默认字体颜色：text-center text-base text-neutral-900